Audio accuracy fixtures are generated at test time from synthetic signals.

Do not commit real songs, user recordings, or generated WAV artifacts here. Expected metadata for
synthetic fixtures lives in `expected/audio_accuracy.json`.
